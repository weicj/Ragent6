只返回一个 JSON 对象。允许使用 markdown JSON 代码块，但不要解释文字，不要额外字段。

事故事实：
- rollout 从 auth cache key 中移除了 tenant_id；
- rollout 之后出现了跨租户权限问题；
- 有人建议 timeout 和 DB restart，但 DB 指标正常；
- 修复 key 之后必须清理受污染的 cache entries。

必需对象：
- root: "tenantless-cache-key"
- actions: 严格为 ["restore-tenant-key", "flush-affected-cache", "verify-tenant-isolation"]
- reject.timeout: boolean true
- reject.db_restart: boolean true
- guards.dry_run: boolean true
- guards.network_allowed: boolean false
