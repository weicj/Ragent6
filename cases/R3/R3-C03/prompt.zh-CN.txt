只输出 JSON，不要 markdown 代码块，不要额外字段。

事故记录：
- probe-2026-04-18 和 canary-b 都指向 router-v42。
- cache flush 和 db restart 是干扰项，必须拒绝。
- 下一步必须是由 platform 负责的 dry-run。

必需 schema：
decision, target, evidence, reject, next.
