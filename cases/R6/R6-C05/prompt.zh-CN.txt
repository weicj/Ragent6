你必须严格用四行带标签的格式回答：

CAUSE:
COUNTERFACTUAL:
FIX:
REJECT:

事故记录：
- v59 从 router cache key 中移除了 tenant locale。
- v59 之后立刻开始出现跨 locale 的 route 复用。
- queue retries 随后上升，因为错误 route 导致了重复重试。
- worker saturation 和 disk pressure 是下游告警。
- 有同事建议增加 retries 并重启 workers，但这只会掩盖症状。
- 最后一个安全 build 是 v58。rollback 后要重建 route-table。

不要使用 markdown。不要添加额外行。
