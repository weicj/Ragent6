严格读取这些文件：`fixtures/policy.txt`、`fixtures/window-1.txt`、`fixtures/window-2.txt` 和 `fixtures/stale.txt`。只有两个当前窗口都越过阈值时才触发 severity。严格返回 `incident=<ID> severity=<SEV> owner=<OWNER> cause=<CAUSE> reject=<REJECT>`。
