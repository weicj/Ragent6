Read exactly these files: `fixtures/policy.txt`, `fixtures/window-1.txt`, `fixtures/window-2.txt`, and `fixtures/stale.txt`. A severity triggers only if both current windows cross the threshold. Return exactly `incident=<ID> severity=<SEV> owner=<OWNER> cause=<CAUSE> reject=<REJECT>`.
