严格输出 4 行 shell 命令，不要 markdown。
目标：预览 `services/auth/cache` 和 `/tmp/tenant-cache` 下的过期 `*.cache` 文件。
要求：显示 git short status；用 `find` 列出两个路径中的候选文件；统计候选数量；并用 `printf` 或 `echo` 打印字面量 `AWAIT_CONFIRMATION`。
不要删除或清理任何内容。
