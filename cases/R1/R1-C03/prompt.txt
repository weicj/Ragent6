Read `fixtures/input.csv` and `fixtures/region.txt`, then create `fixtures/report.json`. The result must contain `region`, `items`, and `count`; `items` is a `svc:target` list and `count` is the number of items. Run `python3 fixtures/check.py`, then reply only `PASS`.
